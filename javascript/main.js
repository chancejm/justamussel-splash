 window.addEventListener('DOMContentLoaded', () => {
    const logo = document.getElementById('logo');
    setTimeout(() => {
      //logo.classList.add('slide-in-elliptic-bottom-bck');
    }, 0); // adjust to match roll-in-left duration
  });